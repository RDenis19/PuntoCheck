# 🏗️ Diagrama de Arquitectura Completa

## Flujo de Datos General

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USUARIO (UI)                              │
│              ConsumerWidget / ConsumerStatefulWidget               │
└────────────────────────────┬────────────────────────────────────────┘
                             │ ref.watch() / ref.read()
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       RIVERPOD PROVIDERS                            │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ StreamProvider:  authStateProvider                          │  │
│  │ FutureProvider:  profileProvider, activeShiftProvider, ...  │  │
│  │ AsyncNotifier:   authControllerProvider,                    │  │
│  │                  attendanceControllerProvider, ...          │  │
│  │ Provider:        currentUserProvider, services, ...         │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────────┘
                             │ ref.read()
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC (SERVICES)                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ • AuthService              • NotificationService             │  │
│  │ • AttendanceService        • ScheduleService                │  │
│  │ • OrganizationService      • BiometricService               │  │
│  │ • StorageService                                             │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────────┘
                             │ Calls
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    SUPABASE BACKEND                                 │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ • PostgreSQL Database      • Realtime Subscriptions         │  │
│  │ • Auth System              • Storage / File Uploads         │  │
│  │ • Row Level Security (RLS)                                   │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Flujo de Autenticación

```
┌──────────────┐
│  LoginView   │
│ (Usuario     │
│  entra)      │
└──────┬───────┘
       │ ref.read(authControllerProvider.notifier).signIn()
       ▼
┌──────────────────────────────┐
│   AuthController.signIn()    │
└──────┬───────────────────────┘
       │ Llama a
       ▼
┌──────────────────────────────┐
│  AuthService.signIn()        │
│  (Supabase Auth)             │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│   Supabase Verifica          │
│   Credenciales               │
└──────┬───────────────────────┘
       │ Emite AuthState
       ▼
┌──────────────────────────────┐
│  authStateProvider           │
│  (StreamProvider observa)    │
└──────┬───────────────────────┘
       │ Trigger de
       ▼
┌──────────────────────────────┐
│  appRouterProvider           │
│  (re-evalúa redirect logic)  │
└──────┬───────────────────────┘
       │ Carga perfil
       ▼
┌──────────────────────────────┐
│  profileProvider             │
│  (Obtiene datos del usuario) │
└──────┬───────────────────────┘
       │ Si es Empleado
       ▼
┌──────────────────────────┐
│  /employee/home          │
│  (Dashboard de empleado) │
└──────────────────────────┘
```

---

## Estructura de Providers por Módulo

### 🔐 Módulo de Autenticación
```
authStateProvider (Stream)
    │
    ├─→ currentUserProvider (Provider)
    │
    ├─→ authControllerProvider (AsyncNotifier)
    │   ├─ signIn()
    │   ├─ signUp()
    │   ├─ signOut()
    │   └─ resetPassword()
    │
    └─→ profileProvider (AsyncNotifier)
        ├─ refresh()
        ├─ updateProfile()
        └─ uploadAvatar()
```

### 👥 Módulo de Organización
```
currentOrganizationProvider (Future)
│
├─→ organizationControllerProvider (AsyncNotifier)
│   └─ updateOrgConfig()
│
├─→ allOrganizationsProvider (Future) [SUPERADMIN]
│
└─→ superAdminStatsProvider (Future) [SUPERADMIN]
```

### ✅ Módulo de Asistencia
```
activeShiftProvider (Future)
│
├─→ attendanceHistoryProvider (Future)
│
├─→ todayStatsProvider (Future)
│
└─→ attendanceControllerProvider (AsyncNotifier)
    ├─ checkIn(location, photo, orgId)
    └─ checkOut(shiftId, location, orgId)
```

### 🔔 Módulo de Notificaciones
```
notificationsStreamProvider (Stream)
│
├─→ unreadNotificationsCountProvider (Provider)
│
└─→ notificationControllerProvider (AsyncNotifier)
    ├─ markAsRead(id)
    └─ markAllAsRead()
```

### 📅 Módulo de Horarios
```
myScheduleProvider (Future)
│
└─→ scheduleControllerProvider (AsyncNotifier)
    └─ createSchedule(schedule)
```

### 🔒 Módulo de Biometría
```
biometricAvailableProvider (Future)
│
└─→ biometricControllerProvider (AsyncNotifier)
    └─ authenticate()
```

---

## Estructura de Rutas

```
┌─────────────────────────────────────────────────────────┐
│                    appRouterProvider                    │
│  ┌───────────────────────────────────────────────────┐ │
│  │ Redirect Logic (5 pasos)                         │ │
│  │ 1. ¿Está autenticado?                           │ │
│  │ 2. ¿Está cargando?                              │ │
│  │ 3. ¿Tiene perfil?                               │ │
│  │ 4. ¿Cuál es su rol?                             │ │
│  │ 5. ¿Tiene permiso?                              │ │
│  └───────────────────────────────────────────────────┘ │
└────────────────────┬────────────────────────────────────┘
                     │
         ┌───────────┼───────────┬──────────────┐
         │           │           │              │
         ▼           ▼           ▼              ▼
      Splash     Login      Register       ForgotPassword
       (/)      (/login)   (/register)     (/forgot/*)
         │           │           │              │
         └─────┬─────┴─────┬─────┴──────────────┘
               │           │
        Logueado?      Perfil cargado?
               │           │
         ┌─────┴─────┐     │
         │           │     │
         ▼           ▼     │ (Si no, espera)
     /employee    /admin
     /superadmin
```

### Rutas Públicas
```
/ (Splash)
/login
/register
/forgot/email
/forgot/code
/forgot/reset
/forgot/success
```

### Rutas de Empleado
```
/employee/home (SHELL)
  ├─ /registro-asistencia
  ├─ /horario-trabajo
  ├─ /historial
  ├─ /avisos
  ├─ /settings
  └─ /personal-info
```

### Rutas de Admin
```
/admin/home (SHELL)
  ├─ /nuevo-empleado
  ├─ /empleados
  ├─ /empleado-detalle/:id
  ├─ /horario
  ├─ /anuncios
  ├─ /anuncios/nuevo
  └─ /apariencia-app
```

### Rutas de SuperAdmin
```
/superadmin/home (SHELL)
  ├─ /organizaciones
  ├─ /organizacion-detalle
  └─ /config-global
```

---

## Ciclo de Vida de una Pantalla

```
┌─────────────────────────────────────────┐
│      1. Usuario navega a ruta           │
│      context.go('/employee/home')       │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  2. Router evalúa permisos               │
│     ¿Usuario logueado?                   │
│     ¿Tiene rol?                          │
│     ¿Tiene permiso?                      │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴────────┐
       │                │
   Sí  ▼                ▼  No
   ┌────────┐      ┌────────┐
   │Permite │      │Redirige│
   └────┬───┘      └────┬───┘
        │               │
┌───────▼──────────────┐
│  3. Renderiza Vista  │
│  (EmployeeHomeView)  │
└───────┬──────────────┘
        │
┌───────▼──────────────────────┐
│  4. Vista construye tree      │
│  (ConsumerWidget)             │
│     - ref.watch() para datos  │
└───────┬──────────────────────┘
        │
┌───────▼──────────────────────┐
│  5. Providers cargan datos    │
│     - Supabase fetch          │
│     - .when() maneja estados  │
└───────┬──────────────────────┘
        │
┌───────▼──────────────────────┐
│  6. UI muestra datos          │
│     - Loading                 │
│     - Data                    │
│     - Error                   │
└───────┬──────────────────────┘
        │
┌───────▼──────────────────────┐
│  7. Usuario interactúa        │
│     - ref.read(controller)    │
│     - Ejecuta acciones        │
└───────┬──────────────────────┘
        │
┌───────▼──────────────────────┐
│  8. Provider se invalida      │
│     - ref.invalidate()        │
│     - Recarga datos           │
└───────┬──────────────────────┘
        │
   (Vuelve a paso 5)
```

---

## Interacción Entre Componentes

```
┌────────────────────────────────────────────────────────────┐
│                     VISTA                                  │
│              (ConsumerWidget)                              │
└────────────────────┬───────────────────────────────────────┘
                     │
           ┌─────────┴──────────┐
           │                    │
           ▼                    ▼
    ref.watch()          ref.read()
    (Reactividad)        (Valor actual)
           │                    │
           ▼                    ▼
    ┌────────────┐        ┌───────────────┐
    │ Provider   │        │ Controller    │
    │ (Lectura)  │        │ (Acciones)    │
    └─────┬──────┘        └────────┬──────┘
          │                        │
          ▼                        ▼
    ┌────────────┐        ┌───────────────┐
    │ Service    │        │ Service       │
    │ (Query)    │        │ (Mutation)    │
    └─────┬──────┘        └────────┬──────┘
          │                        │
          └────────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │   SUPABASE           │
            │  (Database)          │
            │  (Authentication)    │
            │  (Storage)           │
            └──────────────────────┘
```

---

## Flujo de Check-In Completo

```
┌──────────────────────────┐
│  CheckInButton           │
│  (Usuario toca botón)    │
└────────────┬─────────────┘
             │
┌────────────▼──────────────────────┐
│ _handleCheckIn()                  │
│ 1. Obtener ubicación (Geolocator) │
│ 2. Tomar foto (ImagePicker)       │
│ 3. Obtener perfil (profileState)  │
└────────────┬──────────────────────┘
             │
┌────────────▼────────────────────────────┐
│ ref.read(                                │
│   attendanceControllerProvider.notifier  │
│ ).checkIn()                              │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│ AttendanceController.checkIn()           │
│ - Subir foto (StorageService)            │
│ - Registrar entrada (AttendanceService)  │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│ Supabase:                                │
│ - POST /storage/{orgId}/{userId}/...    │
│ - INSERT INTO work_shifts               │
└────────────┬────────────────────────────┘
             │ Success
┌────────────▼────────────────────────────┐
│ Invalidar providers:                     │
│ - ref.invalidate(activeShiftProvider)    │
│ - ref.invalidate(todayStatsProvider)     │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│ Recargar datos automáticamente           │
│ - activeShiftProvider recarga             │
│ - UI se actualiza                        │
│ - Muestra check-out button                │
└────────────────────────────────────────┘
```

---

## Estados de Carga

```
┌──────────────────────────────────┐
│     ref.watch(provider)          │
│     (retorna AsyncValue)         │
└──────────────────────────────────┘
       │
       ├─► .when()
       │
       ├─────────────────────────────────┐
       │                                 │
       ▼                                 ▼
   loading                             data/error
   │                                   │
   ▼                                   ├─► .data()
   Show Spinner                        ├─► .error()
                                       └─► .isLoading
                                           .hasError
```

---

## Tabla de Responsabilidades

| Componente | Responsabilidad |
|---|---|
| **Vista** | Mostrar UI, capturar interacciones |
| **Provider** | Gestionar estado, coordinar servicios |
| **Controller** | Ejecutar acciones, invalidar providers |
| **Service** | Llamadas a Supabase, lógica de negocio |
| **Router** | Navegación, protección de rutas |

---

**Diagrama creado**: Noviembre 2025
**Versión**: 1.0

